import React from 'react'

const PreviousButton = (props) => {
    return <button onClick={props.handleButtonClickBackward}>
            Previous sushi!
          </button>
}

export default PreviousButton